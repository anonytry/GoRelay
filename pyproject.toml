[project]
name = "GoRelay"
description = "GoFile to SourceForge relay via GitHub Actions"
readme = "README.md"
dynamic = ["version"]
requires-python = ">=3.10"
dependencies = [
    "requests>=2.33.1",
]
